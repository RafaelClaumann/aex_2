name: '[FRONTEND] CI'

on:
  workflow_call:
    inputs:
      skip:
        required: false
        type: boolean
        default: false
jobs:
  build:
    if: ${{ inputs.skip != 'true' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Rodando CI de frontend..."
